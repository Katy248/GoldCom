<Window x:Class="GoldCom.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GoldCom"
        mc:Ignorable="d"
        Title="{DynamicResource MainWindowTitle}" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <TabControl 
        Style="{DynamicResource Tabs1}">
        <TabItem x:Name="mainTab" Header="{DynamicResource MainWindowTabMain}">
            <Grid>
                <StackPanel HorizontalAlignment="Center">
                    <Label x:Name="userFullNameLabel" Content="Username"/>
                </StackPanel>
            </Grid>
        </TabItem>
        <TabItem Header="{DynamicResource MainWindowTabRequests}">
            <Grid>
            </Grid>
        </TabItem>
        <TabItem Header="{DynamicResource MainWindowTabCustomers}">
            <Grid>
            </Grid>
        </TabItem>
        <TabItem Header="{DynamicResource MainWindowTabStock}">
            <Grid>
            </Grid>
        </TabItem>
        <TabItem Header="{DynamicResource MainWindowTabUsers}">
            <Grid>
            </Grid>
        </TabItem>
    </TabControl>
</Window>
